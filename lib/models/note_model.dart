import 'package:flutter/material.dart';

class NoteModel {
  String title;
  String content;
  Color color;

  NoteModel({
    required this.title,
    required this.content,
    required this.color,
  });
}
